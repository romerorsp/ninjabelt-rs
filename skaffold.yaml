apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/romerorsp/ninjabelt-rs:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/romerorsp/ninjabelt-rs:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: ninjabelt-rs
        chartPath: charts/ninjabelt-rs
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/romerorsp/ninjabelt-rs"
          image.tag: "{{.DIGEST_HEX}}"
